-- lint-disable-file rule-duplicate-body
-- The single-file CONTROL: deliberately the same program as main.mdk, to prove
-- the gap is loader-specific. The duplication IS the point.
data Foo = Foo Int

main : <IO> Unit
main = println "\{Foo 1}"
