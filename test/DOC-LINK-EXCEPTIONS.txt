# test/DOC-LINK-EXCEPTIONS.txt — checked-in ledger of every KNOWN dead doc
# reference. Read test/check_doc_links.sh first.
#
# This is a RATCHET, not a skip-list. Every line must ACTIVELY EARN ITS PLACE
# on every run: the path is still dead AND something still cites it. When
# either half stops being true, the gate FAILS and the line must be deleted.
# test/check_doc_links.sh enforces all four ways a line can stop earning it:
#
#   1. STALE REF   — the excused path NOW EXISTS. The reference is no longer
#                    dead; leaving the line in would hide a FUTURE regression
#                    at that same path forever.
#   2. ORPHAN REF  — NO DOCUMENT CITES that path anymore. This is the other
#                    half of "no longer needed", and it is the half a naive
#                    skip-list misses. It has already bitten: a docs-cleanup PR
#                    fixed the add-language-feature skill by REMOVING its
#                    citation of test/diff_compiler_roundtrip.sh. The file still
#                    did not exist, so check 1 stayed silent — yet this ledger
#                    still carried a line whose reason said "cited by the live
#                    add-language-feature skill". That reason had become a LIE,
#                    and nothing would ever have reported it. As the docs-cleanup
#                    work drains these paths, EVERY drained entry would rot into
#                    a silent orphan and most of this file would become fiction.
#   3. STALE FILE  — the excused doc no longer exists.
#   4. ORPHAN FILE — the excused doc no longer has a SINGLE dead reference, so
#                    the blanket excuse is dead weight — and would silently
#                    excuse any FUTURE rot in that file.
#
# A ledger nobody re-checks is how test/ported/ and diff_compiler_lint_multi sat
# silently skipped-and-failing for months; this file cannot do that, because the
# gate re-verifies every line on every run.
#
# NOTE: a gate that has to excuse its OWN false positives is a gate with a
# parsing bug. Link targets containing a raw space (DICT-SEMANTICS.md math
# notation) and ellipsis-elided paths (`compiler/.../resolve.mdk`) are therefore
# dropped at EXTRACTION in check_doc_links.sh — not carried as exceptions here.
#
# Format (TAB-separated; blank lines and '#' comments ignored):
#   KIND<TAB>pattern<TAB>reason
#
#   KIND ∈ FILE | REF
#     FILE  pattern is a citing-file shell glob (`case` syntax, e.g.
#           "archive/*" or an exact filename), matched against the file's
#           git-relative path. EVERY dead reference found while scanning a
#           matching file is excused. Use for a doc that is, in its
#           entirety, a point-in-time record — a completed audit/plan, a
#           dated log, a bootstrap narrative — where the correct fix is
#           "leave it alone", not "update the paths": rewriting a closed
#           record's paths to match today's tree would make it describe a
#           tree that never existed at the time the record was written.
#     REF   pattern is the exact resolved target path (root-relative, as
#           this gate normalizes it), matched exactly. Every citation of
#           that dead target, in ANY file, is excused. Use for a one-off
#           stale reference inside an otherwise-LIVING doc — a doc that is
#           still current, actively read, and whose OTHER links must stay
#           held to account.
#
# Reason convention: a reason starting with "TODO(docs-cleanup):" marks a
# REAL bug — a stale path from the compiler subfolder restructure (or a
# comparable rename/removal) sitting in a doc that is still current and
# should say the right thing. Any other reason marks genuine, permanent
# history.
#
# ── Split, re-derived against main @ 3fdd93bb (PRs #8/#10/#12 merged) ────────
#   15 FILE exceptions — all point-in-time records (closed audits/plans,
#       dated logs, the bootstrap narrative, the 6 archive/*.md flat files —
#       listed EXPLICITLY, not as an "archive/*" glob, since shell `case`
#       globbing doesn't stop at `/`: a glob would also swallow
#       archive/design/** and archive/findings/**, which are NEWLY archived
#       (2026-07-13 docs-tree reorg) current docs whose links must stay live).
#       Legitimate history; not counted as debt. All 15 still earn their place.
#    1 REF exception   — compiler/foo.mdk, a deliberately fictional filename in
#       AGENTS.md's own worktree-gotcha example. Not a doc bug.
#   21 REF exceptions  — TODO(docs-cleanup): real stale paths in LIVING docs
#       (was 25 at the split; 4 drained 2026-07-15, see note below). This is
#       the punch list: mostly the compiler/*.mdk -> subfolder move
#       (compiler/typecheck.mdk -> compiler/types/typecheck.mdk etc.) and the
#       retired bytecode stage/gates.
#
#   DRAINED 2026-07-15 (issue #82 row 5 docs-cleanup pass): 4 REF lines whose
#   ONLY living (non-FILE-excused) citation was a genuine subfolder-move typo,
#   now fixed at the source and removed here: compiler/core_ir_sexp.mdk (was
#   cited bare in PLAN.md, now compiler/ir/core_ir_sexp.mdk), compiler/parser.mdk
#   (PLAN.md, now compiler/frontend/parser.mdk), compiler/ir/trmc.mdk
#   (compiler/WASMGC-TRMC-DESIGN.md's own design-alternatives prose — the
#   doc chose compiler/backend/trmc_analysis.mdk; the never-built alternative
#   name is gone from the sentence), and test/diff_wasm.sh
#   (compiler/WASMGC-DESIGN.md — the ledger already flagged this as a
#   within-doc inconsistency with the doc's OWN later correct citation).
#   NOT drained (real, but needs a human call, not a mechanical path swap —
#   see the PR description for detail): the remaining ~17 TODO(docs-cleanup)
#   REF lines whose only living citations sit inside compiler/README.md
#   and/or compiler/STAGE2-DESIGN.md, both of which carry their OWN explicit
#   "historical port-slice log / as-built record, superseded by AGENTS.md /
#   kept as historical record only" banners — rewriting their stale paths
#   would fight the doc's own stated design, not fix a mistake. A few more
#   (compiler/typecheck.mdk's docs/design/CAPABILITY-EFFECTS.md citation,
#   stdlib/bytes.mdk's docs/stdlib/P1-STDLIB-DESIGN.md citations) turned out
#   on inspection to ALREADY correctly narrate the old path as history /
#   a never-built proposal — not bugs, so left untouched; the ledger's own
#   reason strings for those two lines are somewhat mischaracterized as
#   "TODO debt" but re-labeling them changes no gate behavior, so deferred.
#
#   DRAINED by the orphan check when re-derived against merged main (5 lines,
#   all deleted — no longer cited by anything, so no longer earning a place):
#     compiler/frontend/printer.mdk · test/diff_compiler_roundtrip.sh
#     compiler/.../resolve.mdk · D's args · D ā_C   (last three: now dropped at
#     extraction as parser false positives, not excused)
#
#   DRAINED AGAIN 2026-07-13 (docs-tree pass, 5 more lines, base branch had
#   drifted past main since the split above): compiler/CONSTRUCT-COVERAGE.md,
#   compiler/PERF-RESULTS.md, compiler/DISPATCH-GAPS-SCOPE.md (all three FILE
#   exceptions — every dead ref they once excused now resolves), plus REF
#   compiler/private_mangle.mdk (its only citation, compiler/BOOTSTRAP.md, is
#   already FILE-excused, so the REF line shadowed and earned nothing) and REF
#   lib/eval.ml (the bare-path extraction tier only tracks the
#   compiler|stdlib|test|runtime|playground prefixes — lib/ was never matched,
#   so this line could never earn its place no matter how many docs cite it).
#
#   FOLDED 2026-07-13 (docs-tree reorg, PRs #8/#10/#12/#14 status-banner work
#   now landing): the standalone `FILE PLAN-ARCHIVE.md` entry was deleted —
#   PLAN-ARCHIVE.md moved to archive/PLAN-ARCHIVE.md, which the existing
#   `FILE archive/*` glob already covers, so the standalone line was made
#   redundant (and, being a single non-glob path, would have failed the STALE
#   FILE check the moment the move landed).

# ── FILE: point-in-time records (leave the paths as they were when written) ─
FILE	archive/DICT-CONFORMANCE-AUDIT.md	moved-to-archive historical audit (see archive/README.md); closed record, not rewritten
FILE	archive/DICT-CONFORMANCE-ROADMAP.md	moved-to-archive historical roadmap (see archive/README.md); closed record, not rewritten
FILE	archive/EFFECTS-CONFORMANCE-AUDIT.md	moved-to-archive historical audit (see archive/README.md); closed record, not rewritten
FILE	archive/LAYOUT-CONFORMANCE-AUDIT.md	moved-to-archive historical audit (see archive/README.md); closed record, not rewritten
FILE	archive/LAYOUT-CONFORMANCE-ROADMAP.md	moved-to-archive historical roadmap (see archive/README.md); closed record, not rewritten
FILE	archive/PLAN-ARCHIVE.md	completed-Phase archive; narrates the compiler tree as it existed at each phase, including the pre-restructure flat compiler/*.mdk layout and the pre-removal lib/*.ml OCaml reference — rewriting its paths would make it describe a tree that never existed at the time. Moved from root to archive/PLAN-ARCHIVE.md in the 2026-07-13 docs-tree reorg (this line moved with it — NOT a bare "archive/*" glob, which would over-match archive/design/** and archive/findings/**, files that are current and DO get their links kept correct)
FILE	compiler/BOOTSTRAP.md	the B1-B7 + C1-C3 native self-compile bootstrap LOG (AGENTS.md's own description) — narrates each slice as it landed, against the compiler layout at that point in time
FILE	compiler/TYPECHECK-AUDIT.md	dated 2026-06-09 point-in-time audit against the (now-removed) OCaml reference compiler in lib/ — a closed historical record, not a living reference
FILE	compiler/PRE-FLIP-GAPS.md	header: "STATUS: COMPLETE - all 9 items closed 2026-06-12; milestone flip done" — closed punch-list, historical
FILE	compiler/PERF-NOTES.md	header: "Living record ... don't rewrite history, add to it" — the doc's OWN stated convention is append-only; old entries name the paths as they were when measured
FILE	compiler/PERF-SCOPE.md	header: "STATUS: COMPLETE - bar-4 executed 2026-06-11; results in PERF-RESULTS.md" — closed scoping doc, historical
FILE	compiler/DISPATCH-INVENTORY.md	explicitly a GENERATED point-in-time report ("Generated by compiler/entries/dispatch_inventory_main.mdk"), not hand-maintained prose
FILE	compiler/DRIVER-COLLAPSE-PLAN.md	header: "STATUS: COMPLETE - all phases done 2026-06-13" — closed plan, historical
FILE	compiler/MESSAGE-AUDIT.md	a read-only, point-in-time audit of user-facing message strings — historical grading record, not a living reference

# ── REF: not a doc bug (a deliberately fictional example filename) ──────────
REF	compiler/foo.mdk	AGENTS.md's own worktree-gotcha example uses "compiler/foo.mdk" as a deliberately fictional illustrative filename ("a relative `grep -n compiler/foo.mdk` runs there..."), not a real citation

# ── REF: TODO(docs-cleanup) — real stale paths in living docs ───────────────
REF	compiler/ast.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/frontend/ast.mdk
REF	compiler/check.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/tools/check.mdk
REF	compiler/desugar.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/frontend/desugar.mdk
REF	compiler/eval.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/eval/eval.mdk
REF	compiler/exhaust.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/frontend/exhaust.mdk
# (compiler/llvm_emit.mdk's REF exception lived here. The ORCHESTRATING.md terseness
#  pass fixed that doc's stale path to compiler/backend/llvm_emit.mdk — the last LIVING
#  citation — so the ORPHAN REF half of the ratchet correctly demanded this line's
#  deletion. The TODO(docs-cleanup) it named is DONE.)
REF	compiler/loader.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/driver/loader.mdk
REF	compiler/marker.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/frontend/marker.mdk
REF	compiler/resolve.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/frontend/resolve.mdk
REF	compiler/typecheck.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/types/typecheck.mdk
REF	compiler/sexp.mdk	TODO(docs-cleanup): stale path from the compiler subfolder restructure; now compiler/ir/sexp.mdk
REF	compiler/bytecode.mdk	TODO(docs-cleanup): the bytecode pipeline stage was retired entirely (not renamed) — no successor file exists; the citing doc should say so or drop the reference
REF	compiler/entries/llvm_emit_gaps_main.mdk	TODO(docs-cleanup): entry point renamed/removed during the compiler/entries/ reorg; no llvm_emit_gaps_main.mdk exists (see wasm_emit_gaps_main.mdk for the wasm-side survivor)
REF	compiler/entries/mark_main.mdk	TODO(docs-cleanup): entry point renamed/removed during the compiler/entries/ reorg; no mark_main.mdk exists in compiler/entries/
REF	compiler/entries/perf_main.mdk	TODO(docs-cleanup): entry point renamed during the compiler/entries/ reorg; now compiler/entries/profile_main.mdk (or profile_modules_main.mdk)
REF	stdlib/bytes.mdk	TODO(docs-cleanup): planned module from the P1 stdlib design shipped under different names — stdlib/byteparser.mdk and stdlib/bytebuilder.mdk — not stdlib/bytes.mdk
REF	test/diff_compiler_bytecode_eval_dict.sh	TODO(docs-cleanup): gate for the retired bytecode pipeline stage; no successor script exists
REF	test/diff_compiler_bytecode_selfproc.sh	TODO(docs-cleanup): gate for the retired bytecode pipeline stage; no successor script exists
REF	test/diff_compiler_eval_bytecode.sh	TODO(docs-cleanup): gate for the retired bytecode pipeline stage; no successor script exists
REF	test/diff_compiler_eval_bytecode_modules.sh	TODO(docs-cleanup): gate for the retired bytecode pipeline stage; no successor script exists
REF	test/diff_compiler_doc.sh	TODO(docs-cleanup): gate no longer exists under this name (test/doc_fixtures/ is still present; the runner that consumed it is gone or renamed)
