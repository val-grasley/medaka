-- block-form `let` with a type annotation (plain and function-typed)
annotated =
  let p = 5 : Int
  let q = addOne : Int -> Int
  q p

-- mut block-let with annotation
mutAnnot =
  let mut r = 5 : Int
  r = r + 1
  r

-- un-annotated block-let still parses (no regression)
plain =
  let s = 5
  s
