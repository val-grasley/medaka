TYPE ERROR: Impl of 'meth' for interface 'M' requires 'Num' on the method's quantified type variable 'b', but the declared type 'a -> b -> b' does not: every caller instantiates 'b' freely (including to a type with no 'Num' impl), so the body may not assume it. Add the constraint to the method's signature (`Num b => …`), or make the body parametric in 'b'.