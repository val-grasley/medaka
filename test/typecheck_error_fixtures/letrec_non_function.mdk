-- D2: a `let rec` binding whose right-hand side is not a function.
-- Both oracle (tc_probe) and compiler must emit LetRecNonFunction.
-- Oracle: TYPE ERROR: 'f' is bound by 'let rec' but ...
-- Selfhost fix: checkLetRecDecls in checkProgramSeeded (typecheck.mdk).
let rec f = f + 1
