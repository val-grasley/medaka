TYPE ERROR: Effect laundering in impl of 'mk' for interface 'Mk': the body performs <Stdout> inside the method's effect variable <e>, at a position whose declared row does not include it, so a caller can instantiate <e> to <> and a value typed pure would silently perform it. Declare the effect as a concrete label at that position in the method's signature, or perform it only through an argument (a callback carrying <e>) so the caller sees it.