TYPE ERROR: Effect variable <e> in method 'act1' of interface 'Act' carries <Stdout> at an argument position, but a method row carrying <e> does not include it: an impl that uses that argument performs <Stdout> on a row a caller can instantiate to <>, laundering it. Declare <Stdout> (or `<IO | e>`) at every row where <e> appears, or remove the concrete label from the argument's row.