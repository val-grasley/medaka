-- A Map-like tree whose `Bin` constructor has arity 5 (mirrors stdlib/map.mdk).
data Box k v = Tip | Bin Int k v (Box k v) (Box k v)

export singletonBox : k -> v -> Box k v
singletonBox k v = Bin 1 k v Tip Tip

export boxKey : Box k v -> Option k
boxKey Tip = None
boxKey (Bin _ k _ _ _) = Some k
