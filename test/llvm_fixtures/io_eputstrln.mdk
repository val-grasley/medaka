-- native extern catalog slice 3: ePutStrLn — writes string to stderr with newline.
-- stderr is dropped by the gate's 2>/dev/null; a Unit main suppresses the auto-print
-- (d0a99a9), so stdout is empty.  Still proves compile + link + run for the stderr path.
main = ePutStrLn "err"
