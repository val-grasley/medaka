-- slice 9: CRangeList exclusive (half-open).  [1..6] builds a list [1,2,3,4,5]
-- via emitRangeList (alloca-counter back-to-front loop).  Sum = 15.
sumL lst = match lst
  x :: xs => x + sumL xs
  [] => 0

main = sumL [1..6]
