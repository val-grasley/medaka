-- Fixture for rule-not-eq autofix.

notEq a b = a != b
notNe a b = a == b
bangEq a b = a != b
bangNe a b = a == b
nested a b c = if a != b then c else a

-- NEGATIVE: must stay untouched.
negLt a b = not (a < b)
negFlag flag = not flag
negPlain a b = a == b
