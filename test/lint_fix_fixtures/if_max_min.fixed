-- Fixture for rule-if-max-min autofix.

gtNormal a b = max a b
geNormal a b = max a b
ltNormal a b = min a b
leNormal a b = min a b

gtReversed a b = min a b
geReversed a b = min a b
ltReversed a b = max a b
leReversed a b = max a b

nested xs ys = max (len xs) (len ys) + 1

-- NEGATIVE: must stay untouched by the fixer.
negOtherBranch a b c = if a > b then a else c
negClampZero x = if x < 0 then 0 else x

gt a b = a > b
negBacktick a b = if a `gt` b then a else b

negSameOperand a = if a > a then a else a
