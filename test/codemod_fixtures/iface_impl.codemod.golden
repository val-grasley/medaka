-- codemod fixture: <Mut> in an interface method signature AND in an impl's
-- `requires` type argument are both rewritten.
interface Encode a where
  encode : a -> Unit

impl Encode (List a) requires Encode (Int -> a) where
  encode xs = ()
