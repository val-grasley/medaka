issue: 789
what: `medaka check --json` FABRICATES a `range` of `{"start":{"line":0,"character":0},
what: "end":{"line":0,"character":0}}` for a diagnostic that carries NO real source
what: location (`cjRangeOfLoc src None = cjRange 0 0 0 0`,
what: compiler/driver/diagnostics.mdk:886) instead of emitting an absent/null range. An
what: editor consuming `--json` squiggles line 1 col 0 of the file for an error that has
what: nothing to do with that position. The concrete repro is the SAME span-less
what: R-MODULE-LOAD path #333 pins (a nonexistent file path treated as an unknown module
what: name, diagnostics.mdk:619/:992) — #789 is a DIFFERENT defect on the same repro:
what: #333 is about the wrong CODE (R-MODULE-LOAD instead of file-not-found); #789 is
what: about the wrong SHAPE of `range` for ANY unlocated diagnostic, of which this is
what: only one instance. The oracle-compat rationale for `{0,0}` (byte-matching the
what: removed OCaml oracle's dummy loc) is stale since the oracle was removed
what: 2026-06-26.
cmd: check-json missing.mdk
exit: 1
diag-code: R-MODULE-LOAD 0:0-0:0
control: check-json control.mdk
why-fixture: no main.mdk in this directory on purpose — the missing file IS the fixture,
why-fixture: same shape as #333's fixture (`cmd:` names a path that must not exist).
why-control: an EXISTING, valid file must check clean, isolating the observation to the
why-control: unlocated-diagnostic path rather than to `check --json` being broken
why-control: generally.
why-diag-code: the rendered code+range IS the observable: render_diags turns an ABSENT
why-diag-code: `range` key into `None:None-…` (python's dict .get default), so a fix that
why-diag-code: makes the JSON range null/absent changes this line's text and drains the
why-diag-code: row — whereas the CURRENT fabricated `{0,0}` renders as literal `0:0-0:0`,
why-diag-code: which is exactly what's pinned.
why-note: ⚠️ this fixture's diag-code line ("R-MODULE-LOAD 0:0-0:0") is TEXTUALLY
why-note: IDENTICAL to test/must_fail_fixtures/333-check-nonexistent-path-reports-module-load's
why-note: pin, because both bugs currently manifest on the same repro. Whoever fixes #789
why-note: (making the range null/absent) will ALSO flip #333's row red as a side effect —
why-note: that is not a false drain of #333 itself (#333's own defect, the wrong CODE,
why-note: independently needs its own fix), but the fixer should re-verify #333's CODE
why-note: claim still needs work before closing it on the strength of a red run here.
why-note: The two rows are pinning genuinely different defects on a shared repro, per
why-note: AGENTS.md's shared-corpus warning — flagged explicitly rather than silently.
