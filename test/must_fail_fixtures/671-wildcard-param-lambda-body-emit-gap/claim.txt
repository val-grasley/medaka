issue: 671
what: A function whose OUTER param is a wildcard `_` and whose BODY is a lambda
what: (`mk _ = (y => 42)`) fails to emit: the arity-raise/lambda-merge folds it into a
what: 2-arg `mk _ y = 42` but never renames the outer wildcard, so a PWild reaches
what: paramDecls/paramEnv, which only match PVar, and hits a panic. check and run accept
what: it (run prints 42); native `medaka build` exits 1 and produces NO binary.
cmd: build main.mdk
exit: 1
control: build control.mdk
why-control: control.mdk names the outer param (`mk x = (y => 42)`) — the SINGLE trigger,
why-control: `_` vs `x` — and MUST build (exit 0). Normal params (even wildcards) are
why-control: renamed before paramDecls; only the arity-raised outer wildcard slips through.
why-no-observable: exit 1 is pinned; the message is NOT. It is `runtime error [E-PANIC]:
why-no-observable: llvm spike: function parameters must be plain variables in slice 2` — the
why-no-observable: `slice N` index is a volatile emitter internal (and `llvm spike:` is the
why-no-observable: dead-language umbrella #397). Pinning it risks a false drain on an
why-no-observable: unrelated emitter edit; exit 1 + the named-param control discriminates.
