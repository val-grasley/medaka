data Dog = Dog

wrap : (Unit -> String) -> Int -> (Unit -> String, Int)
wrap f n = (f, n)

mk : Unit -> String
mk u =
  let s = "woof"
  s

pair : (Unit -> String, Int)
pair = wrap mk 1

main = putStrLn ((fst pair) ())
