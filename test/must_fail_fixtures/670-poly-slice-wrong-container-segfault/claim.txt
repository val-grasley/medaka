issue: 670
what: The range-slice `xs.[lo..hi]` carries NO Sliceable/Index constraint, so it typechecks
what: on ANY receiver — even an Int. Native lowering dispatches on a STATIC type tag whose
what: catch-all is Array, so `42.[0..1]` compiles to a `loadTag` that `inttoptr`s the
what: immediate Int 42 and dereferences it — reading the never-mapped first page. check
what: accepts (exit 0), run gives a clean coded trap, but the built binary SIGSEGVs: exit
what: 139. (Symptom A in the issue — a polymorphic slice on a String leaking exact heap
what: bytes at exit 0 — is the same root cause and is documented in #670, but its leaked
what: byte pattern is heap-layout/toolchain-dependent, not machine-stable, so it is NOT
what: pinned here; a stable crash code is.)
cmd: build-run main.mdk
exit: 139
control: build-run control.mdk
why-control: control.mdk slices a GROUNDED Array (`[|104, 105|].[0..1]`) — the receiver type
why-control: is known, so the static-tag dispatch is correct and it prints `[|104|]` (exit
why-control: 0) in both engines. The SINGLE distinguishing feature is whether the receiver
why-control: is a real container vs an ungrounded/wrong one. If the control crashes too, the
why-control: build/run ENVIRONMENT broke, not #670.
why-exit-only: exit 139 (128+SIGSEGV) is the pin; a segfault has no stable stdout, so none is
why-exit-only: asserted. This is NOT a bare "nonzero/crashed" pin: 139 is EXACT, and the
why-exit-only: grounded-Array control discriminates an unrelated crash (which would also
why-exit-only: break the control) from this specific wrong-container dispatch. When #670 is
why-exit-only: fixed (a Sliceable constraint added), `42.[0..1]` becomes a CHECK-time type
why-exit-only: error, `medaka build` fails, and build-run returns its build-failure code
why-exit-only: (126) != 139 — so the row DRAINS correctly.
