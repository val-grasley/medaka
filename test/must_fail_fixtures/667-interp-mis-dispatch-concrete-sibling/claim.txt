issue: 667
what: With a concrete `impl Sz Int` AND a general `impl Sz a`, calling `sz` at a type
what: covered ONLY by the general instance (Bool) mis-dispatches in the tree-walking
what: interpreter: run grabs the concrete `Sz Int` impl (prints 111) instead of the general
what: one. Native dispatches correctly (build prints 999). check accepts, both exit 0, so
what: run != build — silent wrongness inside the fully-typed run pipeline (the untyped
what: "first concrete impl wins" arg-tag fallback firing where it must not).
cmd: run main.mdk
exit: 0
stdout: 111
control: run control.mdk
why-control: control.mdk drops the concrete `impl Sz Int`, leaving ONLY the general
why-control: instance — the SINGLE distinguishing feature — so `useSz True` correctly
why-control: resolves to `impl Sz a` and run prints 999 (exit 0). That proves general-
why-control: instance dispatch itself works in run; the defect is specifically the concrete
why-control: sibling stealing the call. Fixing #667 makes main's run print 999, DRAINING
why-control: this row; the control stays 999.
