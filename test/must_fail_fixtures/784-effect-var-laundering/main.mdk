interface Speak a where
  speak : a -> <e> String

data Dog = Dog

impl Speak Dog where
  speak d =
    let _ = putStr "[call] "
    "woof"

pureUse : Speak a => a -> String
pureUse x = speak x

main = putStrLn (pureUse Dog)
