issue: 873
what: An interface-default method body that consumes an ambiguous Num literal
what: body-locally (the literal's element var surfaces nowhere in the method's
what: declared type) has its var grounded to Int by defaultBodyLocalNum ONLY on the
what: impl path (inferImplMethod).  The generic-default path (inferDefaultMethod) has
what: no such grounding.  Which path runs is decided by MODULE PLACEMENT: a same-
what: module impl specializes the default (impl path, grounds), a cross-module impl
what: uses the untagged generic default (default path, does NOT ground).  So the same
what: `t Foo` dispatches `s [1,2]` to a DIFFERENT S impl depending on placement.
what: Here the cross-module program (main.mdk imports tface imports sdisp; impl T Foo
what: in main.mdk) leaves the element var unbound => receiver seen as `List _a` =>
what: the GENERAL `impl S (List a)` is selected => prints 999.  The correct answer is
what: 1 (grounded `List Int` selects the specific `impl S (List Int)`, as the same-
what: module control shows).  check exits 0 with NO diagnostic on both — a silent
what: wrong value keyed on module placement (S0).  #518 reopened on the default path.
cmd: run main.mdk
exit: 0
stdout: 999
control: run control.mdk
why-control: control.mdk is the SAME program (identical S impls, identical T default
why-control: body `t _ = s [1,2]`, identical `impl T Foo`) with interface T and the
why-control: impl co-located in ONE module, so the default is specialized and checked
why-control: via the impl path — whose defaultBodyLocalNum grounds the var to Int and
why-control: selects the specific impl, printing 1.  It exits 0 (green).  The ONLY
why-control: difference from the bug is module placement, isolating that as the
why-control: distinguishing feature.  When the default path gains the missing Num
why-control: grounding, main.mdk will print 1 too, the `stdout: 999` pin fails, and
why-control: this row DRAINS — go close #873.  The control keeps printing 1 throughout.
