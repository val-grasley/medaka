issue: 226
what: a lambda whose body is on the NEXT line is rejected — `f = g x =>` + an indented
what: `g + x` gives `unexpected \`=>\``. docs/spec/LAYOUT-SEMANTICS.md is the designated
what: formal ground truth for layout (§12, :582: "§4–§8 of this file are the definition
what: compiler/frontend/lexer.mdk is held to"). Its herald set (§7.1, :433) includes `=>`,
what: and :437 names THIS EXACT FORM as one that must open a block: "This is why
what: `f x =⏎  body`, `match s⏎  Arm => …`, `g x =>⏎  body`, and `do⏎  stmt` all open
what: blocks". Every OTHER herald in that sentence works today — only `=>` opening a
what: lambda body fails. So the spec is right and the compiler is wrong. S1, 0.1.0.
cmd: check-json main.mdk
exit: 1
diag-code: P-PARSE 0:8-0:9
why-diag-code: diag-code, NOT diag. The DEFECT IS THE REJECTION, not the wording — a legal
why-diag-code: program does not compile. `unexpected \`=>\`` is merely how the parser
why-diag-code: currently voices it, and improving that message while still rejecting must
why-diag-code: NOT drain this row. Any genuine fix makes the program COMPILE (exit 0, zero
why-diag-code: diagnostics), so both the exit and the code flip together.
control: check-json control.mdk
why-control: the SAME lambda with its body on ONE line (`f = g x => g + x`) checks clean.
why-control: That isolates the bug to the LINE BREAK after `=>` — the single distinguishing
why-control: feature — and not to multi-param lambdas, to `=>` generally, or to the binding
why-control: form. If the control breaks, lambdas broke generally and the drain would lie.
why-note: ⚠️ IT IS NOT A LEXER BUG, despite what "lexer-vs-spec divergence" implies. The
why-note: LEXER CONFORMS: `canEndExpr` (lexer.mdk:1355-1369) omits `TFatArrow`, so `=>`
why-note: correctly falls through to False and heralds a block, exactly as §7.1 specifies.
why-note: The defect is a PARSER grammar gap: `lamTailRaw` (parser.mdk:396) parses the body
why-note: with a plain `parseExpr`, and `parseAtomRaw` has NO case for `TIndent`. The source
why-note: says so itself (parser.mdk:~993): "The one herald NOT reachable from the
why-note: expression chain is the bare-INDENT block (it lives only in the decl-body)".
why-note: Top-level bindings escape via `parseBody = orElse indentedBody parseBodyExpr`
why-note: (parser.mdk:2885); lambdas have no equivalent. `orElse` (parser.mdk:239-260)
why-note: then backtracks the WHOLE lambda attempt — including the already-matched `=>` —
why-note: leaving it dangling for the decl parser, which is why the caret lands on the
why-note: `=>` at column 8 rather than on the indented body. Start at parser.mdk:396.
why-note: ⚠️ The bug is NOT "lambdas can't span lines". A lambda in ARGUMENT position with
why-note: its body on the next line ALREADY WORKS — `println (map (x =>⏎  x + 1) [1,2])`
why-note: checks clean, because brackets suspend layout entirely (§6, :305-330). Do not
why-note: widen the pin to bracketed forms; they are green and would never drain.
why-fixture: this row exists because the fork is now RESOLVED. test/MUST-FAIL-NOT-PINNABLE.txt
why-fixture: carried #226 as "CONTESTED, not confirmed... either the lexer is wrong (a bug —
why-fixture: pinnable) or the spec is wrong (a doc bug)... Nobody has made that call. Pin it
why-fixture: the day the fork is resolved in the lexer's disfavour — and delete this line
why-fixture: then." The spec names the failing form verbatim as working and the compiler
why-fixture: rejects it, so it resolves against the COMPILER; that ledger line is deleted in
why-fixture: the same commit as this row.
