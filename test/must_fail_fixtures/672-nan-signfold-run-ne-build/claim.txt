issue: 672
what: The native compile path constant-folds a NaN-producing float op to a DIFFERENT NaN
what: sign than both the interpreter and the native runtime. `0.0/0.0` yields -NaN at
what: runtime (x86 divsd) but clang/LLVM folds the literal `fdiv 0.0, 0.0` to +NaN. Since
what: Float compare is IEEE totalOrder (sign-dependent at NaN), this is an observable
what: same-box run != build: run prints `Lt`, the built binary prints `Gt` (both exit 0).
cmd: build-run main.mdk
exit: 0
stdout: Gt
control: build-run control.mdk
why-control: control.mdk compares two NORMAL floats (`compare 0.0 1.0`) — no NaN, so no
why-control: sign-fold — and prints `Lt` (exit 0) in both engines and stays that way after a
why-control: fix. The SINGLE distinguishing feature is whether a NaN is involved. Fixing
why-control: #672 (canonicalize NaN sign) makes the built main print `Lt`, DRAINING this
why-control: row; the control never moves.
