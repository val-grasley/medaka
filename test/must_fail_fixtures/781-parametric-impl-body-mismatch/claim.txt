issue: 781
what: Follow-up to #777, which was scoped to GROUND impl heads. #780 fixed the ground
what: case by keeping `unify`/`typeMismatch` errors inside `inferUserImplBodies`
what: (compiler/types/typecheck.mdk), but that function filters to GROUND heads
what: (`implHeadGround`), so a PARAMETRIC impl head (`impl Sig (List a)`, not
what: `impl Sig D`) is never body-inferred on the check path. `impl Sig (List a)`'s
what: method body calls the top-level helper `idInt : Int -> Int` with a STRING
what: LITERAL argument (`idInt "str"`), even though the method's own declared return
what: type is `String`. `check` and `build` both accept the program (exit 0, no
what: diagnostics) — the argument-type clash between `idInt`'s `Int` parameter and
what: the `String` argument is silently swallowed during impl-body inference, same
what: as #777, but on a NON-GROUND (parametric) head that #780 does not reach. The
what: built binary then prints `str` at exit 0: `idInt` never actually converts
what: anything — with no type enforcement at the call site it degenerates to passing
what: the String value straight through untyped, so the method's declared `String`
what: return type is satisfied BY ACCIDENT, not by a genuine `Int` result ever
what: existing. Silent wrong value, not a crash, from a program the whole pipeline
what: (check + build + run) calls clean.
cmd: build-run main.mdk
exit: 0
stdout: str
control: build-run control.mdk
why-control: control.mdk is byte-for-byte the same SHAPE — same interface, same
why-control: PARAMETRIC impl head (`impl Sig (List a)`), same `String`-returning
why-control: method body calling a same-arity helper — except the helper is
why-control: `idStr : String -> String`, so the types genuinely agree. It must build
why-control: and run clean, printing the same `str`. That proves the ENVIRONMENT
why-control: (parse/typecheck/build/run pipeline, parametric impl dispatch over
why-control: `List Int`, `String` printing) is fine, and isolates the bug to the
why-control: ARGUMENT-TYPE MISMATCH inside the parametric impl-method body — not to
why-control: parametric impls in general, or to this program's shape.
why-note: This pin does NOT assert that `check`/`build` reject the program — they
why-note: don't, and that IS the bug (see `what:` above). It asserts the OBSERVABLE
why-note: end-to-end consequence instead: the actual bytes the built binary prints.
why-note: A real fix must move this row — either `medaka build` starts rejecting
why-note: main.mdk (build-run's build phase fails, graded as exit 126 by
why-note: test/diff_compiler_must_fail.sh's run_verb, since a binary that never built
why-note: cannot be executed), or the binary's runtime behavior changes — either way
why-note: this exact (exit 0, stdout "str") pin stops holding and the row drains.
why-note: Distinct from #777 (the same bug shape, but on a GROUND impl head — fixed
why-note: by #780; this issue is the parametric case #780 does not reach), #564
why-note: (impl-body Num-literal defaulting, which IS caught via the obligation
why-note: channel), #323 (parametric-impl RKey PAP — a different, literal-only
why-note: path), and #508 (guard-in-impl-method parse, unrelated to typechecking).
