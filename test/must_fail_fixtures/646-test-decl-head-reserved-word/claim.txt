issue: 646
what: `test` used as a top-level DECL HEAD (`test = 2`) is a hard parse error whose
what: message never says `test` is a reserved keyword — the parser commits to the
what: `test "…"` block production, then fails looking for the string label and
what: reports "expected string literal" with the caret on `=`. This is DISTINCT from
what: #532 (binder/let position, fixed by #630's P-RESERVED-KEYWORD): that fix does
what: not cover this decl-head collision site, where the parse doesn't fail to find a
what: binder at all — it succeeds into the wrong production.
cmd: check-json main.mdk
exit: 1
diag: P-PARSE 0:5-0:6 expected string literal
why-diag: THE MESSAGE IS THE DEFECT. Nothing in the output names `test`, "reserved", or
why-diag: "keyword" — a reader looks for a missing string literal, not a reserved-word
why-diag: collision. The range is pinned deliberately: 0:5-0:6 is `=`/what follows it,
why-diag: not the `test` token itself (column 0-3), matching the "wrong token" failure
why-diag: shape #532 first named.
control: check-json control.mdk
why-control: the SAME declaration with `test` renamed to `tst` (a non-reserved word)
why-control: checks clean (exit 0, empty diagnostics). That isolates the bug to the
why-control: WORD `test` at decl-head position, not to the `x = 2` shape generally.
