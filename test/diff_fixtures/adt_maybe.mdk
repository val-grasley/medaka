safeDiv n d =
  match d
    0 => None
    _ => Some (n / d)

showResult r =
  match r
    None => "error"
    Some x => "ok: \{x}"

main : <IO> Unit
main =
  println (showResult (safeDiv 10 2))
  println (showResult (safeDiv 10 0))
  println (showResult (safeDiv 7 3))
