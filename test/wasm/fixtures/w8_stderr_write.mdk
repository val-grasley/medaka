-- W8: ePutStrLn (stderr byte-write) + putStrLn (stdout).  The diff gate compares
-- STDOUT only, so the stdout line must match (stderr goes to fd 2 on both sides).
main =
  let _ = ePutStrLn "diagnostic to stderr"
  putStrLn "visible on stdout"
